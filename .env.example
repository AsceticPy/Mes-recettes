# ============================================
# Configuration Cuisine PWA
# ============================================
# Copiez ce fichier vers .env et modifiez les valeurs

# Configuration de l'application
PORT=8000
USE_DATABASE=true

# Configuration PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cuisine_db
DB_USER=cuisine_user
DB_PASSWORD=CuisineApp2024!SecureP@ss

# Configuration Docker (pour docker-compose)
APP_PORT=8000
